export const environment = {
  production: true,
  // Base URL of the PostgREST API (e.g. https://api.tudominio.com)
  apiUrl: '/api',
  // Nombre del recurso/tabla PostgREST que expone este CRUD
  resource: 'tasks',
};
